name = "terminal_logger"
version = "1.0.0"

description = "Gen_event example for eparch/event_manager: terminal + file loggers"

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
gleam_erlang = ">= 1.3.0 and < 2.0.0"
simplifile = ">= 2.0.0 and < 4.0.0"
eparch = { path = "../../" }

[dev_dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
